// Main exports for the block registry system
export { blockRegistry } from './registry'
export type { 
  BlockDefinition, 
  BlockSchema, 
  BlockProperty, 
  BlockValidation,
  BlockInstance,
  SiteCustomization
} from './types'

// Block definitions
export { heroRuixenBlockDefinition } from './definitions/hero-ruixen-block'

// claude.md followed