interface:
  display_name: "New Features Suggestion"
  short_description: "Suggest and select codebase features in batches"
  default_prompt: "Use $new-features-suggestion to scan this codebase, propose ten features, summarize every option in chat, and let me select them together in Plan mode."
