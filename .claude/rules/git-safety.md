# Git Safety Rules

- When asked to commit, ONLY commit existing changes — never make additional code changes during a commit
- If build fails during commit, report errors and ask before fixing
- Never push or create branches without explicit permission
- Never make additional code changes and bundle them into a commit the user asked for
- Flag secrets in tracked files immediately — don't just skip them, actively warn and fix
