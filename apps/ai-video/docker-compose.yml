services:
  postgres:
    image: postgres:18
    ports:
      - "54322:5432"
    environment:
      POSTGRES_PASSWORD: localdev
      POSTGRES_DB: ai_video
    volumes:
      - pgdata:/var/lib/postgresql

volumes:
  pgdata:
