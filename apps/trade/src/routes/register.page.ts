import { definePage } from "@/lib/pages/page-descriptor"

export default definePage({
  path: "/register",
  name: "Create account",
  summary: "Where a new member signs up.",
  canSwitchOff: false,
})
