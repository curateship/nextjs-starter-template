# Dialog button wording

Editing a wallet ends with Save changes. Creating a market folder uses Create
folder in both the inline form and the management window. The management
window ends with a primary Done button because its changes save as they happen.

These labels follow the shared UI standard. The separate folder-form
consolidation task still owns combining the two creation forms.
