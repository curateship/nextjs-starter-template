services:
  postgres:
    image: postgres:18
    ports:
      - "54323:5432"
    environment:
      POSTGRES_PASSWORD: localdev
      POSTGRES_DB: ai_agents
    volumes:
      - pgdata:/var/lib/postgresql

volumes:
  pgdata:
