import { definePage } from "@/lib/pages/page-descriptor"

export default definePage({
  path: "/login",
  name: "Sign in",
  summary: "Where members sign in with their email and password.",
  canSwitchOff: false,
})
