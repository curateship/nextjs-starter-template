import { definePage } from "@/lib/pages/page-descriptor"

export default definePage({
  path: "/search",
  name: "Search",
  summary: "Search all public content on this site.",
  layout: "marketing",
})
