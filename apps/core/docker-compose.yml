services:
  postgres:
    image: postgres:18
    ports:
      - "54321:5432"
    environment:
      POSTGRES_PASSWORD: localdev
      POSTGRES_DB: core
    volumes:
      - pgdata:/var/lib/postgresql

volumes:
  pgdata:
