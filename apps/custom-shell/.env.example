# Development only
SHADCNBLOCKS_API_KEY=""

CUSTOM_SHELL_R2_ACCOUNT_ID=""
CUSTOM_SHELL_R2_ACCESS_KEY_ID=""
CUSTOM_SHELL_R2_SECRET_ACCESS_KEY=""
CUSTOM_SHELL_R2_BUCKET_NAME="custom-shell-media"
CUSTOM_SHELL_R2_PUBLIC_URL=""

# Absolute base URL used in email links and Stripe redirects. Leave empty in
# local development: it falls back to this app's port in local-apps.json.
CUSTOM_SHELL_APP_URL=""

# Email (Resend). With no key, development logs verification and password reset
# links to the server console instead of sending them.
CUSTOM_SHELL_RESEND_API_KEY=""
CUSTOM_SHELL_EMAIL_FROM="Custom Shell <onboarding@resend.dev>"
# Resend webhook signing secrets are saved per workspace in Settings → Email.

# Bot protection (Cloudflare Turnstile) on sign-up and forgotten-password.
# WITH EITHER OF THESE EMPTY THE CHECK IS SWITCHED OFF ENTIRELY. That is the
# point in local development and a hole in production — set both there.
# Cloudflare's documented always-passes test pair, for local trials:
#   site 1x00000000000000000000AA / secret 1x0000000000000000000000000000000AA
CUSTOM_SHELL_TURNSTILE_SITE_KEY=""
CUSTOM_SHELL_TURNSTILE_SECRET_KEY=""

# "Continue with Google" on the sign-in and sign-up pages. With either of these
# empty the button never appears and nothing else about signing in changes.
# Credentials come from a Google Cloud OAuth client (Web application), whose
# authorised redirect URI must be exactly CUSTOM_SHELL_APP_URL +
# /api/auth/google/callback — locally that is the port in local-apps.json.
CUSTOM_SHELL_GOOGLE_CLIENT_ID=""
CUSTOM_SHELL_GOOGLE_CLIENT_SECRET=""

# Scrambles secrets saved in Settings (the AI provider keys) before they are
# stored in the database. Any string works — longer is better. With it empty,
# saving a key in Settings fails on purpose: keys are never stored unscrambled.
# Changing or losing it makes already-saved keys unreadable; the fix is pasting
# them into Settings again, so pick a value once and keep it.
CUSTOM_SHELL_SECRET_ENCRYPTION_KEY=""

# AI provider keys for the whole app. Optional: keys pasted in Settings -> AI
# are the normal way in and always win over these. A key set here is used only
# while no key is saved in Settings, and the card says so.
CUSTOM_SHELL_ANTHROPIC_API_KEY=""
CUSTOM_SHELL_OPENAI_API_KEY=""
CUSTOM_SHELL_GEMINI_API_KEY=""
CUSTOM_SHELL_ELEVENLABS_API_KEY=""

# Billing. Stripe price ids live on the plan rows in the database, not here.
# Local webhooks: stripe listen --forward-to localhost:<port>/api/webhooks/stripe
CUSTOM_SHELL_BILLING_ENABLED="false"
CUSTOM_SHELL_STRIPE_SECRET_KEY=""
CUSTOM_SHELL_STRIPE_WEBHOOK_SECRET=""
