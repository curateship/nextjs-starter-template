import { definePage } from "@/lib/pages/page-descriptor"

export default definePage({
  path: "/pricing",
  name: "Pricing",
  summary: "The plans on offer and what each one costs.",
  layout: "marketing",
})
