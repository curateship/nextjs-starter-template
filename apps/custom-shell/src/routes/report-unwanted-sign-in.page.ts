import { definePage } from "@/lib/pages/page-descriptor"

export default definePage({
  path: "/report-unwanted-sign-in",
  name: "Report an unwanted sign-in email",
  summary: "Where an unwanted reset or sign-in link is stopped.",
  canSwitchOff: false,
})
