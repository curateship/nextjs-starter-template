import type { ShellStyling } from "@/lib/layout/styling-values"

/** Personal IDE replaces this value in generated apps with the live Custom Shell styling. */
export const scaffoldStyling: ShellStyling | null = null
