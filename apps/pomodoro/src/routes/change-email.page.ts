import { definePage } from "@/lib/pages/page-descriptor"

export default definePage({
  path: "/change-email",
  name: "Confirm email change",
  summary: "Where the link confirming a new account email lands.",
  canSwitchOff: false,
})
